#!/usr/bin/python3
""" 
This module defines text_indentation, a function that replaces the
'?', '.', ':' characters with two newlines.
"""


def text_indentation(text):
    """ Replaces the '?', ',', ':' characters with two newlines """
